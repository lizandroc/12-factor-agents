{
  "name": "12-factor-agents",
  "version": "1.0.0",
  "description": "Monorepo for reconciliation demo app",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix apps/reconciliation-server && npm install --prefix apps/reconciliation-client",
    "dev:server": "npm run dev --prefix apps/reconciliation-server",
    "dev:client": "npm run dev --prefix apps/reconciliation-client",
    "lint": "npm run lint --prefix apps/reconciliation-server && npm run lint --prefix apps/reconciliation-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
